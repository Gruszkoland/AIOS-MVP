apiVersion: v1
kind: Service
metadata:
  name: {{ include "adrion.fullname" . }}-api
  labels:
    {{- include "adrion.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "adrion.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: api
