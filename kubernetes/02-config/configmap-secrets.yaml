---
# ConfigMap for Loki configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: loki-config
  namespace: adrion-369
  labels:
    app: loki
data:
  local-config.yaml: |
    auth_enabled: false
    server:
      http_listen_port: 3100
      log_level: info
    ingester:
      max_chunk_age: 1h
      chunk_idle_period: 10m
      chunk_block_size: 262144
      chunk_retain_period: 1m
      lifecycler:
        ring:
          replication_factor: 1
    limits_config:
      reject_old_samples: true
      reject_old_samples_max_age: 168h
      allow_structured_metadata: false
    schema_config:
      configs:
        - from: 2020-01-01
          store: boltdb-shipper
          object_store: filesystem
          schema: v11
          index:
            prefix: loki_index_
            period: 24h
    storage_config:
      filesystem:
        directory: /loki
      boltdb_shipper:
        active_index_directory: /loki/boltdb-shipper-active
        cache_location: /loki/boltdb-shipper-cache
    table_manager:
      retention_deletes_enabled: false
      retention_period: 0s

---
# ConfigMap for Prometheus configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: adrion-369
  labels:
    app: prometheus
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
      evaluation_interval: 15s
      external_labels:
        cluster: 'adrion-369'
        version: 'v4.0'
    alerting:
      alertmanagers:
        - static_configs:
            - targets:
                - alert-handler:8090
    rule_files:
      - '/etc/prometheus/rules/*.yml'
    scrape_configs:
      - job_name: 'prometheus'
        static_configs:
          - targets: ['localhost:9090']
      - job_name: 'postgres'
        static_configs:
          - targets: ['postgres:5432']
      - job_name: 'loki'
        static_configs:
          - targets: ['loki:3100']
      - job_name: 'n8n'
        static_configs:
          - targets: ['n8n:5678']
      - job_name: 'vortex'
        static_configs:
          - targets: ['vortex:1740']
      - job_name: 'ollama'
        static_configs:
          - targets: ['ollama:11434']
      - job_name: 'api'
        static_configs:
          - targets: ['api:8001']
      - job_name: 'alerter'
        static_configs:
          - targets: ['alert-handler:8090']

---
# ConfigMap for Promtail configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: promtail-config
  namespace: adrion-369
  labels:
    app: promtail
data:
  config.yaml: |
    server:
      http_listen_port: 3101
      grpc_listen_port: 0
    positions:
      filename: /tmp/promtail/positions.yaml
    clients:
      - url: http://loki:3100/loki/api/v1/push
    scrape_configs:
      - job_name: kubernetes-pods
        kubernetes_sd_configs:
          - role: pod
            namespaces:
              names:
                - adrion-369
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_name]
            action: replace
            target_label: pod
          - source_labels: [__meta_kubernetes_namespace]
            action: replace
            target_label: namespace
          - source_labels: [__meta_kubernetes_pod_container_port_name]
            action: keep
            regex: logs

---
# ConfigMap for Nginx configuration
apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-config
  namespace: adrion-369
  labels:
    app: nginx
data:
  nginx.conf: |
    user nginx;
    worker_processes auto;
    error_log /var/log/nginx/error.log warn;
    pid /var/run/nginx.pid;
    events {
      worker_connections 1024;
    }
    http {
      include /etc/nginx/mime.types;
      default_type application/octet-stream;
      log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
      access_log /var/log/nginx/access.log main;
      sendfile on;
      tcp_nopush on;
      tcp_nodelay on;
      keepalive_timeout 65;
      types_hash_max_size 2048;
      gzip on;
      gzip_vary on;
      gzip_proxied any;
      gzip_comp_level 6;
      gzip_types text/plain text/css text/xml text/javascript
                 application/json application/javascript application/xml+rss
                 application/rss+xml font/truetype font/opentype
                 application/vnd.ms-fontobject image/svg+xml;
      limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
      limit_req_zone $binary_remote_addr zone=general_limit:10m rate=100r/s;
      upstream api {
        server api:8001;
      }
      upstream n8n {
        server n8n:5678;
      }
      upstream vortex {
        server vortex:1740;
      }
      upstream grafana {
        server grafana:3000;
      }
      upstream prometheus {
        server prometheus:9090;
      }
      upstream loki {
        server loki:3100;
      }
      server {
        listen 80;
        server_name _;
        location /api/arbitrage {
          limit_req zone=api_limit burst=20 nodelay;
          proxy_pass http://api;
          proxy_set_header Host $host;
          proxy_set_header X-Real-IP $remote_addr;
          proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
          proxy_set_header X-Forwarded-Proto $scheme;
        }
        location /workflows {
          limit_req zone=general_limit burst=50 nodelay;
          proxy_pass http://n8n;
        }
        location /vortex {
          proxy_pass http://vortex;
        }
        location /monitoring {
          proxy_pass http://grafana;
        }
        location /prometheus {
          proxy_pass http://prometheus;
        }
        location /loki {
          proxy_pass http://loki;
        }
        location /health {
          access_log off;
          return 200 "healthy";
          add_header Content-Type text/plain;
        }
      }
    }

---
# WARNING: Replace values before deploying. Use: kubectl create secret generic postgres-credentials --from-literal=KEY=VALUE
# Secret for PostgreSQL credentials
apiVersion: v1
kind: Secret
metadata:
  name: postgres-credentials
  namespace: adrion-369
  labels:
    app: postgres
type: Opaque
stringData:
  POSTGRES_USER: adrion
  POSTGRES_PASSWORD: CHANGE_ME_IN_PRODUCTION
  POSTGRES_DB: genesis_record

---
# WARNING: Replace values before deploying. Use: kubectl create secret generic grafana-credentials --from-literal=KEY=VALUE
# Secret for Grafana credentials
apiVersion: v1
kind: Secret
metadata:
  name: grafana-credentials
  namespace: adrion-369
  labels:
    app: grafana
type: Opaque
stringData:
  GF_SECURITY_ADMIN_PASSWORD: CHANGE_ME_IN_PRODUCTION
  GF_SECURITY_ADMIN_USER: admin

---
# WARNING: Replace values before deploying. Use: kubectl create secret generic n8n-credentials --from-literal=KEY=VALUE
# Secret for N8N credentials
apiVersion: v1
kind: Secret
metadata:
  name: n8n-credentials
  namespace: adrion-369
  labels:
    app: n8n
type: Opaque
stringData:
  N8N_ADMIN_PASSWORD: CHANGE_ME_IN_PRODUCTION

---
# WARNING: Replace values before deploying. Use: kubectl create secret generic alerting-webhooks --from-literal=KEY=VALUE
# Secret for Slack/Pagerduty webhooks
apiVersion: v1
kind: Secret
metadata:
  name: alerting-webhooks
  namespace: adrion-369
  labels:
    app: alert-handler
type: Opaque
stringData:
  SLACK_WEBHOOK_URL: CHANGE_ME_IN_PRODUCTION
  PAGERDUTY_KEY: CHANGE_ME_IN_PRODUCTION
