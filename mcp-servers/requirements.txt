# MCP Servers — ADRION 369
# Model Context Protocol v1.0 — stdio transport
# Compatible with VS Code Claude Code Extension and Claude Desktop

# Core MCP SDK
mcp>=1.3.0

# Data validation (used by shared utilities)
pydantic>=2.0.0

# Async runtime (bundled with Python 3.11+ but pinned for clarity)
anyio>=4.0.0

# Structured JSON logging for GSR events
python-json-logger>=2.0.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
