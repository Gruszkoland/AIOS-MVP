# MCP Servers Requirements
# Model Context Protocol Implementation for ADRION 369 v4.0

# Core
Flask>=3.1.0
Flask-CORS>=6.0.0
requests==2.33.0
python-dotenv>=1.0.0

# Document Processing
python-docx>=1.2.0

# Data & State Management
sqlalchemy==2.0.21
psycopg2-binary==2.9.9
pydantic>=2.4.2
marshmallow==3.26.2
alembic==1.13.1
gunicorn==22.0.0

# Logging & Monitoring
structlog==23.1.0
prometheus-client==0.17.1

# Testing
pytest==9.0.3
pytest-asyncio>=1.3.0
pytest-cov==4.1.0

# Code Quality
black==26.3.1
flake8==6.1.0
mypy>=1.20.2

# Optional: LLM Integration (for ORACLE)
openai>=2.3.0,<3.0.0  # v2 SDK with AsyncOpenAI/OpenAI clients (breaking changes from 0.28.1)
anthropic>=0.7.0,<1.0.0  # Anthropic Claude API
litellm>=1.0.0  # Unified LLM router for multi-model support

# Optional: Redis (for G5TransparencyGuard multi-instance session sync — B5 fix)
# redis>=4.0.0  # pip install redis[hiredis]

# Optional: Browser Automation
browser-use

# Optional: Vector Search & ML (for GENESIS RAG + Federated Learning)
numpy==1.24.3
scipy==1.11.3
hnswlib==0.8.0
sentence-transformers==2.2.2
# faiss-cpu==1.7.4  # Alternative to hnswlib
